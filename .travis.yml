os: linux
dist: bionic

services: docker


before_install:
  - cd alpine-nodejs

script:
- echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_USERNAME" --password-stdin
- docker build -t wajika/alpine-nodejs:latest .
- docker push wajika/alpine-nodejs:latest
